spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hhit?characterEncoding=utf-8&serverTimezone=UTC&useSSL=true
    username: root
    password: hhit

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hhit.demo.domain
  config-location: classpath:mybatis-config.xml

server:
  port: 9090
